/*
steps:
1. download MongoDB
1. add the MongoDB 'bin' directory to the system PATH environment variable if needed
1. start MongoDB using the command prompt; 'mongod --dbpath C:/data/db' in cmd after creating the directory
1. ensure Node.js is installed
2. initialize project; 'npm init -y' in bash
3. install the MongoDB Node.js driver; 'npm install mongodb' in bash
4. create 'app.js' and setup the MongoDB client
5. run the application; 'node app.js' in bash
6. shutdown the MongoDB server (mongod.exe) using windows task manager (end task)
*/