// 1. set up your MySQL database
    // - install MySQL if necessary (as a service for ease of use)
    // - create a database and table to interact with

// 2. set up a Node.js project
    // - create and navigate to project folder
    // - initialize project; 'npm init -y' in bash
    // - install library to interact with MySQL databases; 'npm install mysql2' in bash

// 3. create a js file to handle the MySQL connection 'db.js'

// 4. create a server
    // - install the express library; 'npm install express' in bash
    // - create 'server.js'

// 5. ensure user has privileges

// 6. update 'package.json'

// 7. start the server; 'npm start' in bash

// 8. database manipulation:
    // - get all users; 'curl http://localhost:3000/users' in bash
    // - add a new user; 'curl -X POST -H "Content-Type: application/json" -d '{"name": "John Doe", "email": "john.doe@example.com"}' http://localhost:3000/users' in bash