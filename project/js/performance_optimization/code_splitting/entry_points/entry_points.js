// entry points

// scenario: you have a project with separate entry points for a main application and an admin dashboard

// benefit: users only download the bundle relevant to their current page or feature, reducing the initial payload and speeding up loading