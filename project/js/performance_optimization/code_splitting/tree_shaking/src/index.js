// import only the function that is actually used in this file
// the 'unusedFunction' is not imported, so it won't be included in the final bundle
import { usedFunction } from './utils.js';

// call the function to demonstrate that it works
usedFunction();
