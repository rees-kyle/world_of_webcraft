// use mocha's hooks for setup and teardown:

describe('example tests', function() {
  before(function() {
    // runs once before all tests
  });

  after(function() {
    // runs once after all tests
  });

  beforeEach(function() {
    // runs before each test
  });

  afterEach(function() {
    // runs after each test
  });

  it('should do something', function() {
    // test code
  });
});